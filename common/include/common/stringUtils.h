#pragma once

#include <string>
#include <vector>

namespace gnssShadowing {
namespace common {

    std::vector<std::string> split(std::string str, char delimiter);

} // namespace common
} // namespace gnssShadowing
